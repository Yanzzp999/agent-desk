{
  "name": "agent-desk",
  "version": "0.1.0",
  "description": "Standalone Ralph control plane",
  "private": true,
  "type": "module",
  "bin": {
    "ralphctl": "./bin/ralphctl.mjs"
  },
  "scripts": {
    "serve": "node ./bin/ralphctl.mjs serve",
    "test": "node --test test/*.test.mjs",
    "gui": "node ./bin/ralphctl.mjs gui open --no-open"
  },
  "engines": {
    "node": ">=18"
  }
}
