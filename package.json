{
  "name": "agent-desk",
  "version": "0.1.0",
  "description": "Project task and subagent orchestrator for Codex",
  "private": true,
  "type": "module",
  "main": "src/desktop/main.mjs",
  "bin": {
    "ralphctl": "./bin/ralphctl.mjs"
  },
  "scripts": {
    "dev": "electron ./src/desktop/main.mjs",
    "serve": "node ./bin/ralphctl.mjs serve",
    "test": "node --test test/*.test.mjs",
    "gui": "electron ./src/desktop/main.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "electron": "^42.0.1"
  }
}
